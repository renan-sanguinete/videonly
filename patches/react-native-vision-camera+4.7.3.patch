diff --git a/node_modules/react-native-vision-camera/android/src/main/java/com/mrousavy/camera/core/CameraConfiguration.kt b/node_modules/react-native-vision-camera/android/src/main/java/com/mrousavy/camera/core/CameraConfiguration.kt
index f91ce83..2d68694 100644
--- a/node_modules/react-native-vision-camera/android/src/main/java/com/mrousavy/camera/core/CameraConfiguration.kt
+++ b/node_modules/react-native-vision-camera/android/src/main/java/com/mrousavy/camera/core/CameraConfiguration.kt
@@ -50,7 +50,13 @@ data class CameraConfiguration(
   data class Photo(val isMirrored: Boolean, val enableHdr: Boolean, val photoQualityBalance: QualityBalance)
   data class Video(val isMirrored: Boolean, val enableHdr: Boolean, val bitRateOverride: Double?, val bitRateMultiplier: Double?)
   data class FrameProcessor(val isMirrored: Boolean, val pixelFormat: PixelFormat)
-  data class Audio(val nothing: Unit)
+  data class Audio(
+    val channelCount: Int?,
+    val sampleRate: Int?,
+    val bitRate: Int?,
+    val audioSource: Int?,
+    val gainDb: Double?
+  )
   data class Preview(val surfaceProvider: SurfaceProvider)
 
   val targetFpsRange: Range<Int>?
@@ -124,6 +130,7 @@ data class CameraConfiguration(
       // outputs
       val outputsChanged = left?.photo != right.photo ||
         left.video != right.video ||
+        left.audio != right.audio ||
         left.enableLowLightBoost != right.enableLowLightBoost ||
         left.videoStabilizationMode != right.videoStabilizationMode ||
         left.frameProcessor != right.frameProcessor ||
diff --git a/node_modules/react-native-vision-camera/android/src/main/java/com/mrousavy/camera/core/CameraSession+Configuration.kt b/node_modules/react-native-vision-camera/android/src/main/java/com/mrousavy/camera/core/CameraSession+Configuration.kt
index 1e9c0de..8b0fb2b 100644
--- a/node_modules/react-native-vision-camera/android/src/main/java/com/mrousavy/camera/core/CameraSession+Configuration.kt
+++ b/node_modules/react-native-vision-camera/android/src/main/java/com/mrousavy/camera/core/CameraSession+Configuration.kt
@@ -2,6 +2,7 @@ package com.mrousavy.camera.core
 
 import android.annotation.SuppressLint
 import android.util.Log
+import android.util.Range
 import androidx.annotation.OptIn
 import androidx.camera.core.CameraSelector
 import androidx.camera.core.CameraState
@@ -15,8 +16,10 @@ import androidx.camera.core.TorchState
 import androidx.camera.core.resolutionselector.ResolutionSelector
 import androidx.camera.extensions.ExtensionMode
 import androidx.camera.lifecycle.ProcessCameraProvider
+import androidx.camera.video.AudioSpec
 import androidx.camera.video.Recorder
 import androidx.camera.video.VideoCapture
+import androidx.core.util.Consumer
 import androidx.lifecycle.Lifecycle
 import com.mrousavy.camera.core.extensions.*
 import com.mrousavy.camera.core.types.CameraDeviceFormat
@@ -54,6 +57,7 @@ internal fun CameraSession.configureOutputs(configuration: CameraConfiguration)
 
   val photoConfig = configuration.photo as? CameraConfiguration.Output.Enabled<CameraConfiguration.Photo>
   val videoConfig = configuration.video as? CameraConfiguration.Output.Enabled<CameraConfiguration.Video>
+  val audioConfig = configuration.audio as? CameraConfiguration.Output.Enabled<CameraConfiguration.Audio>
 
   // 1. Preview
   val previewConfig = configuration.preview as? CameraConfiguration.Output.Enabled<CameraConfiguration.Preview>
@@ -127,6 +131,7 @@ internal fun CameraSession.configureOutputs(configuration: CameraConfiguration)
         format?.let { format ->
           recorder.setQualitySelector(format.videoQualitySelector)
         }
+        audioConfig?.let { configureRecorderAudio(recorder, it.config) }
         videoConfig.config.bitRateOverride?.let { bitRateOverride ->
           val bps = bitRateOverride * 1_000_000
           recorder.setTargetVideoEncodingBitRate(bps.toInt())
@@ -338,6 +343,52 @@ internal fun CameraSession.configureSideProps(config: CameraConfiguration) {
   }
 }
 
+private fun configureRecorderAudio(recorder: Recorder.Builder, audioConfig: CameraConfiguration.Audio) {
+  try {
+    audioConfig.audioSource?.let { recorder.setAudioSource(it) }
+
+    val mediaSpecField = Recorder.Builder::class.java.getDeclaredField("mMediaSpecBuilder")
+    mediaSpecField.isAccessible = true
+    val mediaSpecBuilder = mediaSpecField.get(recorder)
+    val configureAudioMethod =
+      mediaSpecBuilder.javaClass.methods.firstOrNull { method ->
+        method.name == "configureAudio" && method.parameterTypes.size == 1
+      } ?: return
+
+    configureAudioMethod.invoke(mediaSpecBuilder, Consumer<AudioSpec.Builder> { builder ->
+      audioConfig.bitRate?.let { builder.setBitrate(Range(it, it)) }
+      audioConfig.sampleRate?.let { builder.setSampleRate(Range(it, it)) }
+      audioConfig.channelCount?.let { channels ->
+        val cameraXChannels =
+          when (channels) {
+            1 -> AudioSpec.CHANNEL_COUNT_MONO
+            2 -> AudioSpec.CHANNEL_COUNT_STEREO
+            else -> null
+          }
+        cameraXChannels?.let { builder.setChannelCount(it) }
+      }
+
+      audioConfig.audioSource?.let { source ->
+        try {
+          builder.setSource(source)
+          Log.i(CameraSession.TAG, "Audio source set to: $source")
+        } catch (e: Exception) {
+          Log.w(CameraSession.TAG, "Failed to set audio source: ${e.message}")
+        }
+      }
+
+      audioConfig.gainDb?.let { gainDb ->
+        Log.i(
+          CameraSession.TAG,
+          "Requested audio gain of ${gainDb}dB. Current CameraX Recorder pipeline does not expose a stable gain setter, so this is best-effort only."
+        )
+      }
+    })
+  } catch (error: Throwable) {
+    Log.w(CameraSession.TAG, "Failed to apply custom audio settings, falling back to defaults.", error)
+  }
+}
+
 internal fun CameraSession.configureIsActive(config: CameraConfiguration) {
   if (config.isActive) {
     lifecycleRegistry.currentState = Lifecycle.State.STARTED
diff --git a/node_modules/react-native-vision-camera/android/src/main/java/com/mrousavy/camera/react/CameraView.kt b/node_modules/react-native-vision-camera/android/src/main/java/com/mrousavy/camera/react/CameraView.kt
index 64a751e..c626f2f 100644
--- a/node_modules/react-native-vision-camera/android/src/main/java/com/mrousavy/camera/react/CameraView.kt
+++ b/node_modules/react-native-vision-camera/android/src/main/java/com/mrousavy/camera/react/CameraView.kt
@@ -59,6 +59,11 @@ class CameraView(context: Context) :
   var photo = false
   var video = false
   var audio = false
+  var audioChannels: Int? = null
+  var audioSampleRate: Int? = null
+  var audioBitRateKbps: Int? = null
+  var audioGain: Double? = null
+  var audioSource: Int? = null
   var enableFrameProcessor = false
   var pixelFormat: PixelFormat = PixelFormat.YUV
   var enableLocation = false
@@ -199,7 +204,16 @@ class CameraView(context: Context) :
 
         // Audio
         if (audio) {
-          config.audio = CameraConfiguration.Output.Enabled.create(CameraConfiguration.Audio(Unit))
+          config.audio =
+            CameraConfiguration.Output.Enabled.create(
+              CameraConfiguration.Audio(
+                audioChannels,
+                audioSampleRate,
+                audioBitRateKbps?.times(1000),
+                audioSource,
+                audioGain
+              )
+            )
         } else {
           config.audio = CameraConfiguration.Output.Disabled.create()
         }
diff --git a/node_modules/react-native-vision-camera/android/src/main/java/com/mrousavy/camera/react/CameraViewManager.kt b/node_modules/react-native-vision-camera/android/src/main/java/com/mrousavy/camera/react/CameraViewManager.kt
index cc20b40..fe12cc7 100644
--- a/node_modules/react-native-vision-camera/android/src/main/java/com/mrousavy/camera/react/CameraViewManager.kt
+++ b/node_modules/react-native-vision-camera/android/src/main/java/com/mrousavy/camera/react/CameraViewManager.kt
@@ -80,6 +80,36 @@ class CameraViewManager : ViewGroupManager<CameraView>() {
     view.audio = audio
   }
 
+  @ReactProp(name = "audioChannels")
+  fun setAudioChannels(view: CameraView, audioChannels: String?) {
+    view.audioChannels =
+      when (audioChannels) {
+        "mono" -> 1
+        "stereo" -> 2
+        else -> null
+      }
+  }
+
+  @ReactProp(name = "audioSampleRate", defaultInt = -1)
+  fun setAudioSampleRate(view: CameraView, audioSampleRate: Int) {
+    view.audioSampleRate = if (audioSampleRate > 0) audioSampleRate else null
+  }
+
+  @ReactProp(name = "audioBitRateKbps", defaultInt = -1)
+  fun setAudioBitRateKbps(view: CameraView, audioBitRateKbps: Int) {
+    view.audioBitRateKbps = if (audioBitRateKbps > 0) audioBitRateKbps else null
+  }
+
+  @ReactProp(name = "audioGain", defaultDouble = 999.0)
+  fun setAudioGain(view: CameraView, audioGain: Double) {
+    view.audioGain = if (audioGain != 999.0) audioGain else null
+  }
+
+  @ReactProp(name = "audioSource", defaultInt = -1)
+  fun setAudioSource(view: CameraView, audioSource: Int) {
+    view.audioSource = if (audioSource >= 0) audioSource else null
+  }
+
   @ReactProp(name = "enableLocation")
   fun setEnableLocation(view: CameraView, enableLocation: Boolean) {
     view.enableLocation = enableLocation
diff --git a/node_modules/react-native-vision-camera/src/types/CameraProps.ts b/node_modules/react-native-vision-camera/src/types/CameraProps.ts
index 18bfc9b..4423cf2 100644
--- a/node_modules/react-native-vision-camera/src/types/CameraProps.ts
+++ b/node_modules/react-native-vision-camera/src/types/CameraProps.ts
@@ -83,6 +83,29 @@ export interface CameraProps extends ViewProps {
    * Note: Requires audio permission.
    */
   audio?: boolean
+  /**
+   * Android only. Configures mono or stereo input for recorded audio.
+   */
+  audioChannels?: 'mono' | 'stereo'
+  /**
+   * Android only. Configures the requested audio sample-rate in Hz.
+   */
+  audioSampleRate?: number
+  /**
+   * Android only. Configures the requested audio bit-rate in kbit/s.
+   */
+  audioBitRateKbps?: number
+  /**
+   * Android only. Requests an input gain adjustment in decibels for the audio capture pipeline.
+   *
+   * Negative values reduce the requested level and can help in loud environments.
+   * This is applied on a best-effort basis because support depends on the underlying CameraX/AudioRecord pipeline.
+   */
+  audioGain?: number
+  /**
+   * Android only. Configures the audio source constant used by the recorder.
+   */
+  audioSource?: number
   /**
    * Specifies the pixel format of Frames streamed from a [Frame Processor](https://react-native-vision-camera.com/docs/guides/frame-processors).
    *
